# NGO Backend

## Setup

1. Create a .env file in this folder and add your Mongo connection string:

   MONGODB_URI=your_connection_string_here
   PORT=5000

2. Install dependencies:

   npm install

3. Run the server:

   npm run dev

Health check: GET /api/health
